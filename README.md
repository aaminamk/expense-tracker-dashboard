# expense-tracker-dashboard
# 💸 Expense Tracker — Smart Dashboard

A mini web-app that allows you to enter expenses in natural text:

> `coffee 1200` → categorized as **Food**

## ✅ Features
- Automatic expense categorization (Food, Transport, Shopping, etc.)
- Dashboard with charts
- Stores data in CSV (like mini database)

## 🚀 How to run locally

```bash
pip install -r requirements.txt
streamlit run app.py
